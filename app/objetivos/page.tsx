import EvolveShell, { Header, Panel } from "@/components/evolve-shell";
import { ChevronRight, Flame, Plus, Scale, Target, Trophy } from "lucide-react";

const goals = [["Llegar a 70 kg", "Peso y medidas", "75,4 kg", "70 kg", "54", "blue", Scale], ["Entrenar 5 veces por semana", "Actividad", "3 sesiones", "5 sesiones", "60", "orange", Flame], ["Mantener 80% de constancia", "Hábitos", "82%", "80%", "100", "green", Trophy]] as const;

export default function ObjetivosPage() { return <EvolveShell active="Objetivos"><Header eyebrow="DIRECCIÓN · JUNIO 2024" title="Tus objetivos" subtitle="Define el siguiente paso de tu evolución." action="Nuevo objetivo" /><section className="goals-highlight"><div className="goals-target"><Target size={18} /><div><p className="eyebrow">OBJETIVO PRINCIPAL</p><h2>Tu evolución está en marcha</h2><p>Has completado el <strong>67%</strong> de tu objetivo de peso.</p></div></div><div className="goal-progress-circle"><strong>67%</strong><small>completado</small></div></section><div className="goals-layout"><section className="goals-list"><div className="section-title"><div><p className="eyebrow">MIS OBJETIVOS</p><h2>Metas activas</h2></div><button className="text-button"><Plus size={12} /> Añadir</button></div>{goals.map(([title, category, current, target, progress, tone, Icon]) => <article className="goal-card" key={title}><div className={`goal-icon ${tone}`}><Icon size={17} /></div><div className="goal-main"><div><strong>{title}</strong><span>{category}</span></div><div className="goal-values"><small>{current}</small><small>Objetivo: {target}</small></div><div className="goal-track"><i className={tone} style={{ width: `${progress}%` }} /></div></div><b>{progress}%</b><ChevronRight size={14} /></article>)}</section><aside className="goal-side"><Panel title="Racha actual" action="Ver historial"><div className="streak-number"><Flame size={20} /><strong>7</strong><span>días</span></div><p className="subtle">Tu mejor racha este mes. ¡Sigue así!</p><div className="streak-days">{["L", "M", "X", "J", "V", "S", "D"].map((day, index) => <span className={index === 6 ? "today" : "done"} key={day}>{index === 6 ? "" : "✓"}<small>{day}</small></span>)}</div></Panel><Panel title="Próximo hito"><div className="milestone"><span>🏁</span><div><strong>73 kg</strong><small>Faltan 2,4 kg para llegar</small></div></div><button className="outline-button">Ver progreso</button></Panel></aside></div></EvolveShell>; }
